BOT_TOKEN=dummy-token-for-pytest
BOT_USERNAME=my_local_test_bot

# Use the same PostgreSQL server as your local app if you want,
# but always use a dedicated test database name.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=my_settlement_test_local

DEVELOPER_IDS=[]
ENABLE_DEVELOPERS_NOTIFY=False

ADMIN_USERNAME=admin_test
ADMIN_PASSWORD=admin_test_password
